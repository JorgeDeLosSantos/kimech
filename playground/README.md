# Playground

This directory contains exploratory mechanism models built with Kimech's current public API.

Unlike the canonical examples in `examples/`, these scripts are intended to probe the limits of the current implementation, experiment with geometries, and evaluate whether a mechanism is useful enough to become an official example later.

A playground script may therefore be more complex, more sensitive to geometry or initial guesses, or change as Kimech evolves.
