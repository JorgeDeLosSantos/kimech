# Kimech

Kimech is a small Python library for modeling and solving the kinematics of planar rigid-body mechanisms.

The project is currently in early development. The design baseline lives in `docs/design.md` and the candidate public API in `docs/api.md`.

## Development install

```bash
python -m pip install -e ".[dev,viz]"
pytest
```
